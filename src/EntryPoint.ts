import { createRblxState } from "./CreateRblxState";

// Import all the required sub-modules and export as the main module.
const rblxStateNameSpace = {
    createRblxState: createRblxState
}

export = rblxStateNameSpace